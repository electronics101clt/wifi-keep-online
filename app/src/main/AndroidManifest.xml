<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Not for drawing anything. On Android 10/11 it is the documented exemption that
         lets the service start the HOME activity from the background. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Declared so the unit's own boot-item picker will list this app.
         com.jancar.settings/.ui.activity.SelfStartSelectorActivity builds its list from
         MAIN/LAUNCHER activities and then drops every non-system app that does not
         request WRITE_EXTERNAL_STORAGE or ACCESS_FINE_LOCATION – its
         HOME_WINDOW_PERMISSION array. Without one of those the app is filtered out
         before the list is drawn, and cannot be set as a startup app at all.

         Declared only. It is never requested at runtime, so no prompt appears and the
         app holds no location access; nothing in the code reads it. Of the two,
         ACCESS_FINE_LOCATION is at least the one Wi-Fi work legitimately needs – it is
         what gates getScanResults() and the real SSID from 8.0 onward. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Translucent.NoTitleBar"
        android:persistent="true">

        <activity
            android:name=".MainActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".WifiWatchService"
            android:exported="false"
            android:stopWithTask="false" />

        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <!-- Cold boot. Rare on a head unit; it sleeps far more than it boots. -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
            <!-- Quick-boot, which ROMs send as part of the boot sequence. The vendor
                 ACC-wake actions are deliberately NOT here: measured on an AC8257,
                 an implicit broadcast never reaches a manifest receiver at targetSdk
                 26, even with the app alive. They are registered dynamically by
                 WifiWatchService instead, where the restriction does not apply. -->
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
